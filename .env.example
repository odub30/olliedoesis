# =====================================================
# PORTFOLIO CONFIGURATION
# =====================================================

# Public Site Configuration
NEXT_PUBLIC_SITE_URL=https://olliedoesis.dev
NEXT_PUBLIC_SITE_NAME="Ollie Does Is - Portfolio"
NEXT_PUBLIC_GITHUB_USERNAME=your-github-username
NEXT_PUBLIC_CONTACT_EMAIL=your-email@example.com

# =====================================================
# DATABASE
# =====================================================

# PostgreSQL Database URL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require
# Get from: Vercel Postgres, Supabase, Railway, Neon, or local PostgreSQL
POSTGRES_DATABASE_URL="postgresql://user:password@localhost:5432/portfolio?sslmode=require"

# =====================================================
# AUTHENTICATION (NextAuth.js v5)
# =====================================================

# NextAuth Configuration
# Generate secret with: openssl rand -base64 32
# For production, use: https://olliedoesis.dev
# For development, use: http://localhost:3000
NEXTAUTH_URL=https://olliedoesis.dev
NEXTAUTH_SECRET=your-secret-key-here-generate-with-openssl-rand-base64-32

# GitHub OAuth Provider
# Create app at: https://github.com/settings/developers
# Production Callback URL: https://olliedoesis.dev/api/auth/callback/github
# Development Callback URL: http://localhost:3000/api/auth/callback/github
GITHUB_ID=your-github-oauth-client-id
GITHUB_SECRET=your-github-oauth-client-secret

# Google OAuth Provider (Optional)
# Create app at: https://console.cloud.google.com/apis/credentials
# Production Callback URL: https://olliedoesis.dev/api/auth/callback/google
# Development Callback URL: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Email Provider (Resend)
# Get API key at: https://resend.com/api-keys
# Used for magic link authentication
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxx
EMAIL_FROM=noreply@olliedoesis.dev

# =====================================================
# OPTIONAL: ADDITIONAL SERVICES
# =====================================================

# GitHub API (for enhanced repository data)
# Create token at: https://github.com/settings/tokens
# GITHUB_TOKEN=ghp_your_personal_access_token

# Analytics (Google Analytics)
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# =====================================================
# SETUP INSTRUCTIONS
# =====================================================
#
# 1. Copy this file to .env.local:
#    cp .env.example .env.local
#
# 2. Update the following REQUIRED variables:
#    - POSTGRES_DATABASE_URL (your database connection string)
#    - NEXTAUTH_SECRET (generate with: openssl rand -base64 32)
#    - GITHUB_ID and GITHUB_SECRET (from GitHub OAuth app)
#    - RESEND_API_KEY and EMAIL_FROM (from Resend dashboard)
#
# 3. Update OPTIONAL variables if needed:
#    - GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET (for Google OAuth)
#    - GITHUB_TOKEN (for enhanced GitHub repo data)
#
# 4. Run database migrations:
#    npx prisma migrate dev
#
# 5. Start the development server:
#    npm run dev
#
# 6. Sign in at https://olliedoesis.dev/auth/signin (or http://localhost:3000/auth/signin for dev)
#    The first user to sign in will automatically become an admin
#
# =====================================================
